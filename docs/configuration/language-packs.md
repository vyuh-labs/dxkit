# Language packs

DXKit ships with 8 first-class language packs. Each pack contributes
everything dxkit needs to analyze a project in that language —
detection, source extensions, test patterns, lint integration,
coverage parsing, dep-vuln scanning, license tooling, and more.

| Pack       | Detected when                                               | Primary tools                                                    |
| ---------- | ----------------------------------------------------------- | ---------------------------------------------------------------- |
| typescript | `package.json` + `*.ts`/`*.tsx`                             | eslint, npm-audit, license-checker, Istanbul                     |
| python     | `pyproject.toml` / `requirements.txt` / `setup.py` + `*.py` | ruff, pip-audit, pip-licenses, coverage.py                       |
| go         | `go.mod` + `*.go`                                           | golangci-lint, govulncheck, go-licenses, gocov                   |
| rust       | `Cargo.toml` + `*.rs`                                       | clippy, cargo-audit, cargo-license, tarpaulin                    |
| csharp     | `*.csproj` / `*.sln` + `*.cs`                               | dotnet-format, osv-scanner-nuget-direct, nuget-license, coverlet |
| kotlin     | `build.gradle.kts` / `build.gradle` + `*.kt`                | detekt, gradle-versions-plugin, gradle-license-report, JaCoCo    |
| java       | `pom.xml` / `build.gradle` + `*.java`                       | pmd, mvn versions, mvn license:third-party, JaCoCo               |
| ruby       | `Gemfile` + `*.rb`                                          | rubocop, bundle-audit, licensee, SimpleCov                       |

## How detection works

DXKit walks the project root once to identify which packs are
**active**. A pack is active when:

1. Its manifest exists (`package.json`, `Cargo.toml`, `pyproject.toml`,
   etc.), AND
2. At least one source file matching its extensions exists outside of
   excluded directories.

Multi-language repos activate multiple packs. A Node + Python repo
runs eslint AND ruff. A polyglot monorepo runs everything that
applies.

## Active packs in your repo

```bash
vyuh-dxkit tools
```

The header of the output groups tools by pack:

```
LAYER 2 (typescript pack active):
  ✓ eslint ...
LAYER 2 (python pack active):
  ✓ ruff ...
LAYER 2 (go pack: not detected in this repo — skipped)
```

## What each pack contributes

A `LanguageSupport` interface declares everything a pack provides:

- **Source extensions** (`.ts`, `.tsx`, `.cs`, etc.)
- **Test-file patterns** — pack-specific conventions
  (`*.test.ts` / `tests/*.rs` / `*_test.go` / `test_*.py` / etc.)
- **Autogenerated source patterns** — basename globs to exclude
  (`*.designer.cs`, `*.pb.go`, etc.)
- **Documentation comment patterns** — `///`, `##`, `/**`, varies
- **TLS-bypass regex patterns** — language-specific idioms
- **cloc language names** — what cloc calls this language
- **Capability providers** — typed channels for lint, depVulns,
  coverage, imports, testFramework, licenses

This means **adding a new pack auto-extends every dxkit report**.
The graphify scan, the hygiene grep, the BoM aggregation, the
test-gap discovery — they all iterate `LanguageSupport` and pick up
the new contributions automatically.

## Forcing pack activation

If detection misses a pack (rare — typically a missing manifest), you
can force-activate via `dxkit.yml`:

```yaml
languages:
  python: true # force on
  go: false # force off
```

The detection result is still computed and shown for reference; the
force-flag overrides activation only.

## What if my pack isn't listed?

The 8 first-class packs cover the common cases. If your project uses
a language outside the list (Elixir, Crystal, Zig, etc.) dxkit will
still:

- Count source files (via cloc, which knows most languages)
- Run gitleaks (language-agnostic)
- Run semgrep (with whatever cross-language rules apply)
- Skip the per-pack contributions (no language-specific lint, no
  dep-vuln scan)

The per-language depth requires a pack. To add a 9th, see the
[contributing](../../CONTRIBUTING.md) guide — adding a pack is a
one-command scaffold (`npm run new-lang`) and filling in TODOs.

## See also

- [`.dxkit-ignore`](dxkit-ignore.md) — overrides what counts as a
  source file (exclusions)
